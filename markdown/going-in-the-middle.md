# Going in the middle

For the third test post, I am creating a filename in between `c` and `i`. This manual test should appear first in the list.

And it works!

**Edit (two asterisks)**: Rewrote with `md5sum`s to check if a file should be converted.